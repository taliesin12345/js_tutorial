// Various exercises, moved here from other files.

// (Exercise) Splits username and domain in an email address.
function emailSplitter(email) {
  return emailArray = email.toLowerCase().split("@");
}
